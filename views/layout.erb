<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Shopping</title>
  <link rel="stylesheet" href="/assets/stylesheets/bootstrap.css">
</head>
<body>
    <%= yield %>
    ここはログインしなくても見れる
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#defaultNavbar1"><span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span></button>
     </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="defaultNavbar1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="/signin">ログイン<span class="sr-only">(current)</span></a></li>
        <li><a href="/signup">ユーザー登録</a></li>
      <form class="navbar-form navbar-left" action="/add_item" method="post">
        <div class="form-group">
          <input type="text" class="form-control" name="name" placeholder="商品名">
          <input type="text" class="form-control" name="image_url" placeholder="画像url">
<!--           <input type="text" class="form-control" name="description" placeholder="description"> -->
          <input type="text" class="form-control" name="price" placeholder="price">
        </div>
        <button type="submit" class="btn btn-default">アイテムを追加</button>
      </form>
      <li><a href="/signup">カートの中身を見る</a></li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
<div class="row">
<% Item.all.each do |item| %>
  <div class="col-md-3">
    <div class="thumbnail"><img src="<%= item.image_url %>" alt="Thumbnail Image 1" style="width:200px;">
      <div class="caption">
        <h3><%= item.name %></h3>
        <p><%#= item.description %></p>
        <p><a href="/add_to_cart" class="btn btn-primary" role="button">カートに入れる</a></p>
      </div>
    </div>
  </div>
<% end %>
</div>




  </body>
</html>
